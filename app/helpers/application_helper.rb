module ApplicationHelper
  # In case of using inherited_resources gem it would be better to have such helper
  # for places where some actions toolbar can be usefull
  def actions_toolbar(resource)
  end
end
